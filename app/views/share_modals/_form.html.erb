<%= form_for(@share_modal) do |f| %>
  <% if @share_modal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@share_modal.errors.count, "error") %> prohibited this share_modal from being saved:</h2>

      <ul>
      <% @share_modal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :slug %><br>
    <%= f.text_field :slug %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
